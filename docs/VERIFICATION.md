# 验证记录

## 0.1.29 发布前验收（进行中，2026-09-01）

- 归档证据进入 workspace schema 2；专项回归覆盖旧 schema／三个兼容 JSON 一次迁移、二次启动不重复迁移、变化摘要链防篡改、权威提交后兼容镜像写入中断、提醒通知不推进成功时间、失败／离线运行状态保留，以及带报告摘要的上下文导出。
- 隔离临时目录验证健康文件完整读取、同大小内容变化、文件缺失、目标离线和不完整基线拒绝。修复专项覆盖来源重新哈希、损坏原件保留、发布后回读，并在复制后、保留原件后、发布后三处注入故障；临时文件被清理且损坏原始字节仍可恢复。
- 类型检查、生产构建、补丁检查、高危依赖审计和媒体来源门禁通过；全量回归为 34 个测试文件、278 项通过，4 个文件／4 项按硬件或压力环境跳过。代码复审另修复异常中断恢复明细丢失，以及已由后续成功修复／完整复校验关闭的旧差异继续被误列为当前风险的问题。
- 使用仓库外隔离工作区实际启动开发候选：界面显示 0.1.29，旧记录迁移到 schema 2；项目复校验实时完成为 0／1 个任务健康，并显示操作人、失败结论和证据摘要。权威状态保存 1 次运行和 4 条哈希链接变化；失败运行只更新风险与目标在线状态，没有推进下次提醒或伪造最后成功时间。
- 实际打开“从健康副本修复”重要确认，界面准确列出任务、目标、候选来源、目标卷身份、2 个文件和 131073 字节，以及保留损坏原件与中途失败边界；本次验收选择取消，界面即时显示已取消，隔离素材未发生写入。软件内 0.1.29 说明只有用户点击后显示，20 个模块全部默认折叠。
- 新的包内归档证据脚本已在本机 arm64 构建产物中运行，完成 65536 字节完整读取、损坏识别、修复发布、损坏原件保留和证据摘要检查。它将在候选与标签流水线中分别由原生 arm64／原生 Intel 再次执行。
- 本地 arm64 与 x64 DMG 均通过镜像校验，版本为 0.1.29，主程序分别为 arm64／x86_64；两包 `app.asar` SHA-256 均为 `8f58e75322c7d2175a51348ee93c72ce87c6d6aa54921df0ad2a37ab58263842`。本地 DMG SHA-256：arm64 `99cb723871b0cbd8b72c5461dc27044499e8ee41d1b60079856ac3b4ef67ff57`，x64 `b88fb588e9b4e2e135d2af7897d1d2993b99577eb5a2049be3036b794ebf0e50`；它们不是最终 GitHub Release 附件。
- 从两份只读挂载的 DMG 直接运行四组包内专项：arm64 原生与 x64 Rosetta 均通过三哈希传输／恢复、媒体编解码、代理证据／原子交付，以及 65536 字节归档完整读取、损坏识别、修复发布和损坏原件保留。两包媒体二进制架构正确，摘要分别为 arm64 `388c97bb09f54ca4efbd64bdc822e9adb7aebf4aa3f73b949993f3379fd3d11c`、x64 `1473d8e78105a64280f4b65f3efbd61c6d9ebc186ce341000c1cd7b1f04c13f0`；Rosetta 不冒充原生 Intel。
- 安装包内归档证据检查脚本已加入原生 arm64／原生 Intel 流水线门禁；当前仍待分支 CI、标签 CI、最终附件回下载和在线 Release 核对，本节不代表公开发行。
- 全部写入验证仅使用仓库外临时合成数据，没有读取、迁移、修改或删除用户真实素材、生产项目、原始 MHL 或长期归档记录。

## 0.1.28 最终发布验收（2026-09-01）

- 标签 `v0.1.28` 指向提交 `f7230c0`；标签流水线 [33515484122](https://github.com/sexyfeifan/Kocpy/actions/runs/33515484122) 成功。原生 arm64 与原生 Intel 均完成 264 项回归、4 项条件跳过、来源门禁、未签名 DMG 构建以及包内传输／恢复、媒体和代理证据／原子交付专项；双架构通过后只创建一个未公开草稿。
- 草稿四项附件完整回下载；首次 GitHub CLI 连接在约八成处中断，随后按资产 ID 从精确字节位置续传。最终大小、GitHub 资产摘要、本地 SHA-256 与 `SHA256SUMS.txt` 全部一致：arm64 DMG 为 `533b1f55f87cae91a801350ea503bcb5a2d8ac2f767630300e63bbddea4a996e`，x64 为 `433b7a168788ca480d6d6f5b9d622d83a9b96e9bdb7f881f49598a1efcd849db`，媒体对应源码为 `9f90284461b9aa957ee40f8a8520fd1f8f93176e0f276fe35253c33504bd138b`，摘要文件为 `527d8f888e088208466c2d7afe849b70cba617eda1b2f5f51b8f794908a8358d`。
- 两份最终 DMG 均通过镜像校验，只读挂载版本为 0.1.28，主程序分别为 arm64／x86_64；两包 `app.asar` 均为 `e84bfdcaa6d2d46faa7dd97ab2c2a029533dfb9a19738358f5e39e461cbed3c2`，内置双架构 FFmpeg 的架构和摘要一致。最终附件从 DMG 直接再次通过三哈希传输／恢复、媒体编解码及代理证据／原子交付专项；本机 x64 复核使用 Rosetta，原生 Intel 证据来自标签 CI。
- `v0.1.28` 于 `2026-09-01T14:24:07Z` 公开并成为 latest；远端 `main`、标签和 Release 均对应已验收提交，四项附件公开地址、大小与摘要保持不变。没有覆盖标签、替换附件、强推或删除旧发布。

## 0.1.28 发布前本地验收（2026-09-01）

- 新代理任务冻结来源任务、相对路径、字节数、原哈希算法／校验值、媒体信息和完整参数；转码前完整重读源，完成后记录输出 SHA-256、字节数与媒体属性。专项覆盖同大小源替换、预取消、输出篡改、ProRes 非 MOV、重复 basename、未知元数据和交付发布边界。
- 隔离合成样本生成 H.264 1280×720 与 Apple ProRes 422 Proxy 1920×1080，均保留 25 fps、2 秒时长、`01:00:00:00` 起始时间码和单音轨。修复 FFmpeg 输出段重复描述流导致单音轨计为双音轨的问题；旋转等未提供字段保持未知，不填入默认值。
- 两个合成代理已在本机 DaVinci Resolve Studio 20.2.3 实际导入并识别上述编码、分辨率、帧率、时长、时间码和音轨。验证使用唯一临时项目，完成后关闭并删除，未打开生产项目。Premiere Pro 与 Final Cut Pro 未安装；相应 CSV／FCPXML 只完成结构、路径和 XML 解析检查，未记作实际导入通过。
- 正式交付预检会重新完整哈希输出、拒绝重名，并以同目录独占暂存、复制后 SHA-256、文件／目录同步和原子改名发布。回归确认包内清单引用最终 `Media` 路径，失败不会留下正式目录；旧代理缺少输出证据时被拒绝并要求重新生成。
- 本地类型检查、生产构建、补丁检查与高危依赖审计通过；全量回归为 31 个测试文件、264 项通过，4 个文件／4 项按硬件或压力环境跳过。
- 使用仓库外 `/tmp` 隔离数据实际启动开发候选并通过渲染调试通道检查：1080×720 下代理范围、兼容性说明、交付按钮、长路径、源／输出证据和内部滚动可用；代理弹窗标题／底栏固定，ProRes 自动锁定 MOV；软件内 0.1.28 说明只有点击后显示，20 个模块全部默认折叠。
- 本地 arm64 与 x64 DMG 均通过镜像校验，版本为 0.1.28，主程序架构分别为 arm64／x86_64；两包 `app.asar` SHA-256 均为 `1a343a71c3de3d5c50638aaa548e82fed8d9abb337772e0217a83379a60e766c`。本地 DMG SHA-256：arm64 `f6b064da4523ae97e50ff0a409596212fe3c5347be5b39ab62af818912e879dd`，x64 `c579653de9165e64908d752421f1d7548c1fe29a8a52ff6ead4f3277031602b8`；它们不是最终 GitHub Release 附件。
- arm64 原生与 x64 Rosetta 包内运行时均通过 SHA-256／SHA-1／MD5 传输恢复专项、H.264／ProRes 媒体专项及新的代理证据／原子交付专项。两个打包候选均以隔离数据进入 1080×720 代理队列并显示 0.1.28、输出证据和兼容性边界；Rosetta 不冒充原生 Intel，仍以标签 CI 原生 Intel 为最终架构证据。
- 提交 `0f20f8f` 的双架构预发布流水线 [33511254310](https://github.com/sexyfeifan/Kocpy/actions/runs/33511254310) 成功：原生 arm64 与原生 Intel 均完成 264 项回归、4 项条件跳过、来源门禁、生产构建、DMG 打包及三组包内运行时专项。分支运行按规则跳过聚合发布步骤，没有创建或公开 Release。
- 两份 CI 候选附件已完整回下载且摘要与 GitHub Artifact 记录一致：arm64 压缩附件为 `f5b1fc8c6f0459a944fc2989cd101a2f81c355e1feda4f492718e98a9cc5bdfd`，x64 为 `5b0a0d90a65622b7f8d2f5c0d27082bf1b182a14639df17f049d23e745177669`。其中 arm64 DMG SHA-256 为 `ea40bb2f7e858eaa7070f9961cc7b1289b6a31086fe7460e42094443affe2cb0`，x64 为 `56f804600d4b245da205065bd937b4dd45c551c9b5d4fc9fde338cda7f29585b`；两份镜像均通过 `hdiutil verify`，只读挂载版本为 0.1.28，主程序分别为 arm64／x86_64。
- 两个 CI 候选包的 `app.asar` 摘要均为 `e84bfdcaa6d2d46faa7dd97ab2c2a029533dfb9a19738358f5e39e461cbed3c2`；内置 arm64／x64 FFmpeg 摘要分别为 `388c97bb09f54ca4efbd64bdc822e9adb7aebf4aa3f73b949993f3379fd3d11c` 与 `1473d8e78105a64280f4b65f3efbd61c6d9ebc186ce341000c1cd7b1f04c13f0`，架构与名称相符。回下载包在本机再次直接通过三哈希传输／恢复、媒体编解码与代理证据／原子交付专项；x64 本机复核运行于 Rosetta，原生 Intel 证据来自上述 CI。
- 本版仍无 Developer ID 签名／公证；真实摄影机 RAW、Premiere、Final Cut、NAS、拔盘、断电、睡眠、空间耗尽和全部文件系统矩阵未全面验证。标签流水线、最终附件回下载和 GitHub Release 核对完成前，本节只代表本地候选验收。

## 0.1.27 最终发布验收（2026-09-01）

- 最终标签 `v0.1.27` 指向 `3b0432e`；标签流水线 [33495985971](https://github.com/sexyfeifan/Kocpy/actions/runs/33495985971) 成功。原生 arm64 与原生 Intel 各完成 256 项回归、4 项条件跳过、来源门禁、未签名 DMG 构建及包内传输／恢复和媒体运行时专项；双架构通过后只创建一个未公开草稿。
- 草稿四项附件完整回下载，GitHub 资产摘要、本地重算与 `SHA256SUMS.txt` 一致。最终 arm64 DMG SHA-256 为 `048de54b2b65db29e2e52aaa7bc5b860192c291435914ffd76665423bfc47786`，x64 为 `f96cb1b0b510213b550fcd88c626f15d99feec25b50d3975f5d082291e00ac44`，媒体对应源码为 `d94d19f3b10fed37bc11fe4e0460dad8df9e3f3667276baec4652a0290c84a88`，摘要文件为 `f25327b5943cdc3d3e574885cb66dfd95918e032faa586f568c4ef3ef9b20858`。
- 两份最终 DMG 均通过镜像校验，只读挂载版本为 0.1.27，主程序架构分别为 arm64／x86_64；两包 `app.asar` 均为 `0dc63b8c2f28be94008566f8829cf76e2e297ee463abf31f91fcac5f25d62a98`，与已验收候选完全一致。最终 arm64 原生与 x64 Rosetta 附件再次通过三哈希、磁盘身份、只读恢复、失败目标重试、镜像根目录、暂停恢复、双目标独立回读、复校验及全部媒体专项；原生 Intel 证据来自标签 CI，Rosetta 不冒充 Intel 实机。
- 媒体对应源码中的锁文件、构建脚本、FFmpeg、x264 与 NASM 归档摘要可独立验证，并与仓库标签输入一致。`v0.1.27` 于 `2026-09-01T10:19:32Z` 公开并成为 latest；远端主分支、标签和 Release 均指向已验收提交，没有覆盖标签、替换附件、强推或删除旧发布。

## 0.1.27 发布前本地验收（2026-09-01）

- 权威工作区复用规范化序列化结果；索引 schema 6 加入跟踪任务、项目、文件和实体表的持久化 dirty 触发器、修订／摘要完全匹配的干净启动快速路径及磁盘回退点。故障注入覆盖事务发布失败后当前进程和重新打开均恢复旧状态；同文件数的路径或大小改写仍会触发完整漂移核对。
- 素材库使用与项目、搜索和类型绑定的不透明稳定游标；回归覆盖相同创建时间下跨页无重复／遗漏、错误作用域拒绝及最多 16 个并发路径探测。代理调度回归确认仅“备份优先”暂停会自动恢复，用户手动暂停保持不变。
- 本地类型检查、生产构建、补丁检查和高危依赖审计通过；全量回归为 30 个测试文件、256 项通过，4 个文件／4 项按硬件或压力环境跳过。本节仍是发布前候选证据，不替代标签流水线与最终附件复核。
- 固定的仓库外隔离合成基准：十万文件工作区首次提交约 2.08 s、活动检查点约 369 ms、滞后索引恢复重开约 2.00 s、干净重开约 822 ms，规范化状态约 34.2 MB；十万文件索引构建约 1.17 s、峰值常驻内存约 912 MB。百万文件索引构建约 15.9 s、峰值仍约 7.83 GB，这是尚未解决的 `sql.js` 资源边界。
- 使用仓库外 `/tmp` 隔离数据实际启动开发候选，未连接或操作生产项目：版本显示 0.1.27；205 个合成素材条目按 100／100／5 三页展示，第一页到第三页、上一页及搜索范围重置均无重复或遗漏，在线副本状态正确。内置说明只在点击“使用说明”后出现，0.1.27 更新内容可见，全部模块保持默认折叠。代理让路的状态转换由回归和代码复审验证，尚未把真实媒体转码与真实素材盘并发冒充为 GUI 硬件验收。
- 本地 arm64 与 x64 DMG 均通过 `hdiutil verify`，只读挂载版本均为 0.1.27，可执行文件分别为 arm64／x86_64；两包 `app.asar` SHA-256 均为 `56b6b9ad4bc6ececf8fe11dee080fe5b0c9e2d41db4cf097b32b71108e6d5704`。arm64 包内 FFmpeg 为 arm64 且摘要 `388c97bb09f54ca4efbd64bdc822e9adb7aebf4aa3f73b949993f3379fd3d11c`，x64 为 x86_64 且摘要 `1473d8e78105a64280f4b65f3efbd61c6d9ebc186ce341000c1cd7b1f04c13f0`。
- 两个本地包内运行时均通过 SHA-256／SHA-1／MD5、只读恢复检查、失败副本重试、镜像根目录、进度、暂停继续、双目标独立回读、源与 MD5 证据、重新校验及媒体编解码专项。arm64 从只读 DMG 直接加载同一隔离工作区并显示 0.1.27；x64 Rosetta 能进入 0.1.27 窗口且包内专项通过，但本轮桌面会话锁定前没有完成工作区 GUI 交互，因此不把它记录成已通过的 x64 GUI 验收。
- 提交 `155c25d` 的双架构预发布流水线 [33493884557](https://github.com/sexyfeifan/Kocpy/actions/runs/33493884557) 成功：原生 arm64 与原生 Intel 均完成 256 项回归、4 项条件跳过、生产构建、DMG 打包及包内传输／恢复和媒体运行时专项。工作流在分支手动触发，聚合发布步骤按规则跳过，没有创建或公开 Release。
- 两份 CI 候选附件已完整回下载并通过 `hdiutil verify`；只读挂载版本均为 0.1.27，架构分别为 arm64／x86_64。arm64 DMG SHA-256 为 `d2af7785b306e316ca66ab2ba72bc43b22b76ab65df475cd58fc6add53f564bb`，x64 为 `ecd896a5bc25a363a600e28c238647ec2d621ef42baff14060ca056a90a216d8`；两包 `app.asar` 均为 `0dc63b8c2f28be94008566f8829cf76e2e297ee463abf31f91fcac5f25d62a98`，其中 7 个生产输出文件与候选提交本地构建逐字节一致。CI 的干净依赖树没有本机 iCloud 在 `node_modules` 生成的 ` 2` 重复文件，因此整体 `app.asar` 摘要与本地候选不同；差异已逐项确认仅来自这些非业务重复依赖文件，最终发布以干净 CI 包为准。
- 当前本地候选 DMG SHA-256：arm64 `316d9df01f90015f80186a0a62e29d27518719dfea1c26dd09f18c2127930324`，x64 `51ba3d2b7a44a6f8060d93c0f52a7e43801ebfebf7b589f1a70fe83fc3647161`。它们不是最终 GitHub Release 附件。
- 上述数字不是外接素材盘、NAS 或真实片场吞吐。复制哈希、逐目标独立回读、落盘同步、源变化检查与物理独立副本判断没有降低；本版仍未完成 Developer ID 签名／公证，以及真实 NAS、拔盘、断电、睡眠、空间耗尽和全部文件系统矩阵。

## 0.1.26 最终发布验收（2026-09-01）

- 最终标签 `v0.1.26` 指向 `2ce1578`；标签流水线 [33489304011](https://github.com/sexyfeifan/Kocpy/actions/runs/33489304011) 成功。原生 arm64 与原生 Intel 各完成 250 项测试、4 项条件跳过、来源门禁、未签名 DMG 构建及包内传输／恢复和媒体运行时专项；聚合步骤在双架构均通过后创建单一未公开草稿。
- 草稿包含 arm64 DMG、x64 DMG、媒体对应源码和 `SHA256SUMS.txt` 四项附件。GitHub 资产摘要与摘要文件逐项一致：arm64 为 `aa725ca4f7502a7bb6f8d83f917567f731c371de16054516bd6e887a71a34629`，x64 为 `2c3763cab8100a8ef2482e17a240d4cce32b3cf442b375fa8b63398eef1d1758`，媒体对应源码为 `0f4c19ab5461fa7557d4ca121d0c69fbc114561fbf30e95da792c1ed0788d13c`，摘要文件为 `75cfc96e3257d37cd439aa3fe09464f6ba2e7b797e03fe38b20188d2e8130665`。
- `v0.1.26` 于 `2026-09-01T08:58:59Z` 公开并成为 latest；公开 Release、远端标签与本地标签均指向已验收提交，附件名称、大小、摘要和下载地址与草稿一致。没有覆盖标签、替换附件、强推或删除旧 Release。
- 发布写入只涉及 Kocpy 仓库、标签、公开说明和 Release；测试继续使用仓库外隔离合成数据，没有读取、迁移或修改生产项目、真实素材或原始清单。当前仍无 Developer ID 签名或公证；真实 NAS、拔盘、断电、空间耗尽和全部文件系统矩阵未在本版完成。

## 0.1.26 发布前本地验收（2026-09-01）

- 任务与项目统一进入带格式版本、单调修订、SHA-256 摘要和删除墓碑的权威工作区；SQLite 只保存同修订的可重建索引。故障注入覆盖首次迁移、显式空 JSON、SQLite 补位、并发双域提交、主／备份／索引恢复、索引漂移、删除防复活、权威／镜像／索引写入中断、同修订有效冲突、较新兼容标记、未知格式和全部权威副本损坏。
- 类型检查、生产构建和补丁检查通过；全量回归为 29 个测试文件、250 项通过，4 个文件／4 项按硬件或压力环境跳过；`npm audit --audit-level=high` 为 0 个已知漏洞。工作区专项为 22 项通过，其中包括文件总数不变但路径／大小被改写的逻辑索引漂移修复。
- 隔离性能基准使用合成记录：1 万文件首次权威提交约 265 ms、活动检查点约 129 ms、重启恢复约 283 ms；10 万文件一次测量为首次提交约 3.8 s、只写权威活动检查点约 0.8 s、重启并核对索引约 6.0 s。数值受当前机器和缓存影响，不冒充真实外接盘传输性能。
- 使用仓库外 `/tmp` 隔离数据启动开发候选：0.1.25 风格的 1 个任务／1 个项目 JSON 首次启动后生成工作区修订、迁移记录、兼容标记和 SQLite 索引；项目与任务在 GUI 正确显示。强制中断后再次启动仍为修订 2、迁移修订 1，未重复迁移。软件内说明只有点击后出现，全部模块默认折叠；没有读取或修改生产 Kocpy 数据。
- 本地 arm64 与 x64 DMG 均通过 `hdiutil verify`，只读挂载版本均为 0.1.26，可执行文件分别为 arm64／x86_64；两包 `app.asar` SHA-256 均为 `03a8fe75586b6a5879b636ad2aa912ce93d06e4965de530d6073f445d5d2da56`。arm64 包内 FFmpeg 为 arm64 且摘要 `388c97bb09f54ca4efbd64bdc822e9adb7aebf4aa3f73b949993f3379fd3d11c`，x64 为 x86_64 且摘要 `1473d8e78105a64280f4b65f3efbd61c6d9ebc186ce341000c1cd7b1f04c13f0`。
- 从两份只读挂载的本地 DMG 直接运行应用：arm64 原生与 x64 Rosetta 均显示 0.1.26 并进入工作台。Rosetta 结果不冒充原生 Intel；标签发布前仍须由双架构 CI 补齐原生 Intel 测试和构建证据。
- 当前本地候选 arm64 DMG SHA-256 为 `4646c2743527518dea2bb35887eff7c63dff6652e4d81968f49efa47edf847b0`，x64 为 `453efe2c4fa5b8e88b3187d9b8ffd377a38a7ae68551b5320eb2ccab1c9ebb99`。它们是发布前候选，不是最终 GitHub Release 附件；最终附件必须在标签流水线后重新下载和核对。
- 当前无有效 Developer ID Application 证书，候选未使用 Developer ID 签名、未公证。真实 NAS、拔盘、断电、空间耗尽和全部文件系统矩阵没有在本轮完成，不据此宣称现场风险已全部关闭。

## 0.1.25 最终发布验收（2026-09-01）

- 建立全应用 GUI 设计系统：按钮、表单、选择器、状态、空页面、表格、弹窗和检查器共享尺寸、间距、焦点、禁用原因与深浅主题变量；侧栏字体和图标不再随窗口高度缩小，空间不足时由导航和正文分别滚动。
- 普通弹窗和危险确认共用焦点栈。结构回归覆盖安全初始焦点、Tab／Shift-Tab 循环、Escape 关闭条件、焦点返回、脏表单离开保护、项目菜单锚点、非模态媒体检查器，以及全部输入控件具有可访问名称。
- 类型检查、生产构建和补丁检查通过；全量回归为 28 个测试文件、228 项通过，3 个文件／3 项按既有可选环境或压力条件跳过；`npm audit --audit-level=high` 为 0 个已知漏洞。
- 本地 arm64 与 x64 DMG 均通过 `hdiutil verify`，只读检查版本均为 0.1.25，可执行文件分别为 arm64／x86_64；两包 `app.asar` SHA-256 均为 `0f29539ab37ea977c0604a44c79eb5f2d13b856292b8e692186640cba5775e6d`。本地 arm64 DMG SHA-256 为 `479ba134261ec3591ac91d4a20095831e1522dff16f44b1a5a8b45f91defab7b`，x64 为 `af317b5aeba0a09107f0cbcc8303bb5bb60dd10aec8c90cb201f0590968d5608`。
- 本机 arm64 原生和 x64 Rosetta 打包应用均通过 SHA-256／SHA-1／MD5、磁盘身份、只读恢复检查、失败目标重试、镜像根目录、块内进度、暂停／恢复、双目标独立回读、源与 ASC MHL 哈希、复校验旧状态清理，以及元数据、缩略图、波形、AAC、H.264、ProRes 和预取消媒体专项。Rosetta 结果不冒充 Intel 实机；0.1.25 原生 Intel 候选证据仍须由后续 CI 补齐。
- 使用仓库外隔离数据运行打包后的 arm64 应用：实际检查默认深色、浅色、1080 × 720 最小窗口和放宽窗口；侧栏导航可独立滚动且字号／图标稳定，软件内完整说明只有点击后出现且各模块默认折叠。
- 在隔离目录建立单日／单设备合成项目，实际检查项目卡片、操作菜单和项目详情。删除确认默认聚焦风险确认框，Shift-Tab 循环到取消，取消后焦点返回“更多项目操作”；没有执行删除或备份。项目详情在最小窗口中保持表格、状态按钮、长文案和底部操作可达，深浅主题均未发现遮挡或异常拉伸。
- 最终标签 `v0.1.25` 指向 `591f9c6`；标签流水线 [33480016533](https://github.com/sexyfeifan/Kocpy/actions/runs/33480016533) 成功。原生 arm64 与原生 Intel 各完成 228 项测试、3 项条件跳过、来源门禁、未签名 DMG 构建及包内传输／恢复和媒体专项，聚合步骤创建单一未公开草稿。
- 草稿 4 项附件已完整回下载，`SHA256SUMS.txt`、GitHub 资产摘要与本地重算一致；两份 DMG 通过镜像校验，只读挂载版本均为 0.1.25，架构分别为 arm64／x86_64。最终 arm64 DMG SHA-256 为 `a90c50b14936cc203dd65f141a25c0c6935fe24f1dfc6163ace38bab6a29061d`，x64 为 `dfb700e69519e6bbe98d725925b2fb99b6e7a781a775510fd80167ea07968b3a`，媒体对应源码为 `09d64bc7d62755bb394a4b195247af3dc877ea07b05316532c7a7c0b930cce28`，摘要文件为 `23e792aea4073953eb9cd84dc60e04d596d782acec96c94db36451dd83759ac2`。
- 两个最终包的 `app.asar` 摘要均为 `0f29539ab37ea977c0604a44c79eb5f2d13b856292b8e692186640cba5775e6d`，与已实际完成深浅主题、最小／宽窗口、侧栏独立滚动、项目卡片／详情和危险确认键盘流程检查的候选包完全一致。最终 arm64 原生与 x64 Rosetta 附件再次通过三哈希、磁盘身份、只读恢复、失败目标重试、镜像根目录、暂停恢复、双目标独立回读、复校验及全部媒体专项；原生 Intel 证据来自标签 CI。生产版 Kocpy 当时正在运行且应用实施单实例锁，没有为重复打开最终包而关闭生产实例；以相同业务包摘要和最终附件运行时复核组成等价 GUI 证据。
- 媒体对应源码的锁文件、构建脚本、FFmpeg／x264／NASM 归档摘要及构建信息一致；包内双架构媒体二进制与对应构建摘要一致，GPL／LGPL／x264 许可文本齐全。标签 CI 已完成 FFmpeg 上游签名来源门禁；本机没有 `gpg` 命令，因此未把本机未执行项写作再次通过。
- `v0.1.25` 于 `2026-09-01T07:11:16Z` 公开并成为 latest；公开 Release 指向已验收标签，4 项附件名称、大小、摘要和下载地址与草稿完全一致，全部地址支持范围下载。没有覆盖标签、替换附件或删除旧 Release。
- 全部写入测试使用生成数据和隔离目录，没有读取、迁移或修改用户生产项目、真实素材或原始清单。仍无 Developer ID 签名或公证；未完成真实 NAS、拔盘、断电和全部相机格式验证。

## 0.1.24 最终发布验收（2026-09-01）

- 接管预览新增稳定扫描摘要、逐卷映射与阻断问题；专项测试覆盖未识别日期／设备、用户修正映射、两个目录映射为同一逻辑卷时拒绝、目录内容变化后摘要改变。
- 相同哈希的不同路径合并时保留双方接管审计；同一组记录连续整理三次，素材卷数保持 `1 → 1 → 1`，副本位置不重复增加。
- 接管写入前后复扫、物理卷身份记录、追加式基线／清单／重定位证据和“旧副本在线时关联另一健康副本”已完成代码复审。离线来源继续保留为未知，未降低外部清单或物理独立副本门槛。
- 当前类型检查、生产构建、补丁检查和高危依赖审计通过；专项 54 项通过；首次全量回归为 27 个测试文件、221 项通过，3 个文件／3 项按既有可选环境或压力条件跳过。
- 使用仓库外 `/tmp` 隔离数据实际操作 0.1.24 开发构建：整项目接管预览正确显示两个素材卷及逐卷映射；把两个目录改成相同日期／设备／机位／卷名后即使勾选关联确认也无法开始，恢复唯一映射后才允许接管。接管完成显示两个逻辑素材卷、两条已验证记录、零个满足两份物理副本要求，未把同盘目录误报为独立副本；任务详情显示操作人、规范化源路径、扫描摘要和接管审计。
- 在同一隔离项目中连续执行三次“刷新接管信息”。每次预览均为 2 条接管记录、0 条目录修正、0 条重复、0 条父级汇总、0 份清单差异、0 条待建基线；三次完成后仍为 2 个逻辑素材卷、2 条任务和 62 B，总量没有膨胀。项目追加式事件为 1 次接管加 3 次刷新，两个任务各自的原始接管审计保持不变。
- 软件内说明实际显示 0.1.24 更新内容，所有说明模块保持默认折叠。
- 提交 `c79bdab` 的双架构预发布流水线 [33471193060](https://github.com/sexyfeifan/Kocpy/actions/runs/33471193060) 成功：原生 arm64 与原生 Intel 各完成 221 项测试、3 项条件跳过、生产构建、DMG 打包及包内传输／恢复和媒体运行时专项。手动触发没有标签，因此没有创建或公开 Release。
- 两份候选 DMG 已完整回下载并通过 `hdiutil verify`；只读挂载版本均为 0.1.24，可执行文件分别为 arm64／x86_64。候选 arm64 DMG SHA-256 为 `9e57a6aa7f32c24bf6f14c1d3f2ee129aed6586eafc087c537fe800b06b5d8b6`，x64 为 `7df0be256373603bb66c5824ab0978fc3d8de5c14bf0431f14a425c070c9d8b5`；两包 `app.asar` 摘要均为 `6cd6478a30003b3bb24928391d4b9cb84d4e5b017a00af8f4989d5ba019ee145`，其中 7 个构建输出文件与候选提交本地生产构建逐字节一致。
- 候选 arm64 原生和 x64 Rosetta 均再次通过 SHA-256／SHA-1／MD5、记录磁盘身份、只读恢复检查、失败目标重试、镜像根目录、块内进度、暂停／恢复、双目标独立回读、源与 ASC MHL 哈希、复校验旧状态清理，以及元数据、缩略图、波形、AAC、H.264、ProRes 和预取消媒体专项。Rosetta 不冒充 Intel 实机，原生 Intel 证据来自上述 CI。
- 使用回下载的候选 arm64 DMG 和 `/tmp` 隔离数据实际确认版本、两个逻辑素材卷、接管审计、默认折叠说明和三次刷新后的稳定计数。另从素材库选择同盘合成健康副本：全卷预检后原生确认框明确显示新位置、文件数、再次完整读取和同盘计数规则；确认后二次哈希通过，素材库显示 2／2 历史校验且 2 份可访问，项目仍为 0／2 单元达到两份物理独立副本要求，关联路径、操作人和事件进入追加式审计。
- 最终标签 `v0.1.24` 指向 `c195393`；标签流水线 [33478383351](https://github.com/sexyfeifan/Kocpy/actions/runs/33478383351) 成功。原生 arm64 与原生 Intel 各完成 221 项测试、3 项条件跳过、DMG 构建及包内传输／恢复和媒体专项，聚合步骤创建单一未公开草稿。
- 草稿 4 项附件已完整回下载，`SHA256SUMS.txt`、GitHub 资产摘要与本地重算一致；两份 DMG 通过镜像校验，只读挂载版本均为 0.1.24，架构分别为 arm64／x86_64。最终 arm64 DMG SHA-256 为 `a2dc0385b0e83479e17dd6a3793af0a1e2af10b248ae73f33443b8e2697fb783`，x64 为 `763cd1ddae2a7c295a8f021a438fada659f89ceb5fea5f0575e8339d8332f9ac`，媒体对应源码为 `c4144b87adf479ac357d47a47b253113d18d50f13c2fa15afca2b3c4f8dc45a7`，摘要文件为 `293236ceb18853895fe47d3fd97c62c1214a9d6877ce1e3fb6532879031fe74f`。
- 两个最终包的 `app.asar` 摘要均为 `6cd6478a30003b3bb24928391d4b9cb84d4e5b017a00af8f4989d5ba019ee145`，与已实际完成接管、三次幂等刷新、同盘健康副本关联及默认折叠说明检查的候选包完全一致。最终 arm64 原生与 x64 Rosetta 附件再次通过三哈希、磁盘身份、只读恢复、失败目标重试、镜像根目录、暂停恢复、双目标独立回读、复校验及全部媒体专项；原生 Intel 证据来自标签 CI。由于生产版 Kocpy 当时正在运行且应用实施单实例锁，没有为重复打开最终包而关闭生产实例；以相同业务包摘要和最终附件运行时复核组成等价 GUI 证据。
- 媒体对应源码的锁文件、构建脚本、FFmpeg／x264／NASM 归档摘要及构建信息一致；包内双架构媒体二进制与对应构建摘要一致，GPL／LGPL／x264 许可文本齐全。当前机器没有 `gpg` 命令，FFmpeg 上游签名复核沿用标签 CI 已通过的来源门禁，未把本机未执行项写作再次通过。
- `v0.1.24` 于 `2026-09-01T06:54:43Z` 公开并成为 latest；公开 Release 指向已验收标签，4 项附件名称、大小、摘要和下载地址与草稿完全一致，全部地址支持范围下载。没有覆盖标签、替换附件或删除旧 Release。
- 全部写入测试使用临时生成目录，不读取、迁移或修改用户生产项目、真实素材或原始清单。仍无 Developer ID 签名或公证；未完成真实 NAS、拔盘、断电和全部相机格式验证。

## 0.1.23 最终发布验收（2026-09-01）

- 新增项目规则、每日设备决定、模板应用和交接证据的追加式记录；专项回归覆盖旧规则不可变、无规则变化不制造新版本、操作人必填、非法设备路径拒绝、模板修订、工作空间证据合并和单日交接范围。
- 同一素材卷的失败目标重试与恢复使用稳定逻辑身份；项目覆盖、收工矩阵和素材卷明细按逻辑素材卷统计，不把操作尝试累加成新卡卷。已成功目标在只重试失败目标时保持不变。
- 全量类型检查、生产构建和补丁检查通过；27 个测试文件、218 项测试通过，3 个文件／3 项按既有可选硬件或压力条件跳过。复制回归曾捕获首次失败尝试缺少结束时间，修正为所有任务终态统一封存且重新校验不改写旧尝试后，完整复制引擎 33 项及再次全量均通过；`npm audit --audit-level=high` 为 0 个已知漏洞。
- 使用仓库外隔离数据目录和合成项目实际操作开发构建：验证未填写操作人时保持未知，填写后即时反馈；加入单日临时设备；应用带修订号模板后旧每日决定继续引用旧规则；保存全项目交接后显示规则版本、卡卷、待处理及待确认冻结摘要。默认窗口中操作区、矩阵和反馈可见。
- 提交 `2b04c7a` 的双架构预发布流水线 [33463796754](https://github.com/sexyfeifan/Kocpy/actions/runs/33463796754) 成功：原生 arm64 与原生 Intel 各完成 218 项测试、3 项条件跳过、生产构建、DMG 打包及包内传输／恢复和媒体运行时专项。工作流手动触发，因此只生成候选构建附件，没有创建或公开 Release。
- 两份候选 DMG 已完整回下载并通过 `hdiutil verify`；只读挂载后版本均为 0.1.23，可执行文件分别为 arm64／x86_64。候选 arm64 DMG SHA-256 为 `94081368a369e3d01e91bb981c39d91eda55637c0b54380b063338f630f43bfa`，x64 为 `23c343857a7c991ded365ca98def9a28fe5290e201d94f61553b99e16caa19b2`；两包 `app.asar` 摘要均为 `031841c990cb50db0fa0d76bbfff7a30664eee375214d3b94ea00be5e4aa3198`，其中 7 个构建输出文件与候选提交本地生产构建逐字节一致。
- 候选 arm64 原生与 x64 Rosetta 包均通过 SHA-256／SHA-1／MD5、记录磁盘身份、只读恢复检查、失败目标重试、镜像根目录、块内进度、暂停／恢复、双目标独立回读、源与 ASC MHL 哈希及复校验旧状态清理；媒体专项通过元数据、缩略图、波形、AAC、H.264 mp4/mov/mkv、ProRes mov 与预取消检查。Rosetta 结果不冒充 Intel 实机，原生 Intel 证据来自上述 CI。
- 解锁后运行上述回下载的候选 arm64 DMG，数据目录、项目和目的地均位于 `/tmp` 隔离区，未安装或打开生产版 Kocpy。实际完成每日计划操作人、明确缺失、单日临时 `Drone / Aerial`、规则 v1、全项目交接、纪录片模板差异预览与操作人确认；界面即时显示每日决定、冻结的规则/素材卷/待处理/待确认交接摘要，以及模板应用后的检查表。默认受约束窗口中侧栏、模板卡片、交接和下一步操作可见且可滚动。
- 最终标签 `v0.1.23` 指向 `b6592ef`；标签流水线 [33466281350](https://github.com/sexyfeifan/Kocpy/actions/runs/33466281350) 成功。原生 arm64 与原生 Intel 各完成 218 项测试、3 项条件跳过、DMG 构建及包内传输／恢复和媒体专项，聚合步骤创建单一未公开草稿。
- 草稿4项附件已完整回下载，`SHA256SUMS.txt`、GitHub资产摘要与本地重算一致；两份DMG通过镜像校验，只读挂载版本均为0.1.23，架构分别为arm64／x86_64。最终arm64 DMG SHA-256为`eedde572d6d62259356908d38c563c1ad80a0ee66c44ea2bae6d5967500fc913`，x64为`393213342db5a0ef5652669d240108510f7273a9d19893246c2cf60de5c86d74`，媒体对应源码为`1bfb73bd87fcc4caaec8939520499943fd773ee373640a1283dd25ad64f458ea`，摘要文件为`4fb8569a14bd9a6deb911aa7822871c572901f4c62e67b982bcd822f07164b84`。
- 两个最终包的`app.asar`摘要均为`031841c990cb50db0fa0d76bbfff7a30664eee375214d3b94ea00be5e4aa3198`，与标签构建的7个输出文件逐字节一致；媒体对应源码的锁文件、FFmpeg／x264／NASM归档和构建脚本摘要一致。最终arm64原生与x64 Rosetta附件再次通过三哈希、磁盘身份、只读恢复、失败目标重试、暂停恢复、双目标独立回读、复校验及全部媒体专项；原生Intel证据来自标签CI。
- 解锁后直接运行从草稿回下载的最终arm64 DMG，继续使用`/tmp`隔离合成数据。实际确认0.1.23、规则v2、2条每日决定、1次模板应用、旧FX3决定与单日`Drone / Aerial`仍保留、全项目交接冻结摘要，以及默认折叠的软件内0.1.23使用说明；未安装或打开生产版Kocpy。
- `v0.1.23` 于 `2026-09-01T04:12:04Z` 公开并成为latest；公开Release指向已验收标签，4项附件名称、大小、摘要和公开下载地址与草稿完全一致，全部地址支持范围下载。没有覆盖标签、替换附件或删除旧Release。
- 全部写入测试使用生成文件和隔离目录，没有修改用户生产项目、素材或原始清单。规则和交接为本地审计证据，不冒充第三方数字签名。仍无 Developer ID 签名或公证；未完成真实 NAS、拔盘、断电和全部相机格式验证。

## 0.1.22 最终发布验收（2026-09-01）

- 最终标签 `v0.1.22` 指向 `4fc480b`；标签流水线 [33458728648](https://github.com/sexyfeifan/Kocpy/actions/runs/33458728648) 成功。原生 arm64 与原生 Intel 各 204 项通过／3 项条件跳过，并分别完成 DMG 构建、打包后传输／恢复运行时和媒体编解码专项；聚合步骤生成单一未公开草稿，没有由单架构任务提前公开不完整版本。
- 草稿的4项附件已完整回下载，`SHA256SUMS.txt`、GitHub 资产摘要与本地重新计算结果一致；两份 DMG 均通过 `hdiutil verify`。只读挂载后版本均为0.1.22，可执行文件分别为 arm64／x86_64；两包 `app.asar` 摘要均为 `d1fb4b58d11385a4dd4230b70d59426f22416a706cb1f55513d17bd215f36189`，其中主进程、preload、HTML、JS、CSS及图片与标签提交的构建逐字节相同。
- 最终 arm64 DMG SHA-256：`de7109cfd56f2068b3caba9537a6699f48a7ed220156a8332b088d8dd0e03750`；最终 x64 DMG：`4122b88b1d4aa19d93b94e193d686e5dac05ea8ce5acbebe467efc6e98a444a3`；对应源码包：`cb052b8c74d942d3336daf498b7238ed2ebb7f292f893a8761e64cd71bfe9ffb`。
- 最终包内媒体二进制、固定源码摘要、GPL许可、动态库和网络禁用门禁通过；对应源码附件可脱离Kocpy仓库独立验证输入。最终打包应用在本机以arm64原生及x64 Rosetta运行三哈希、只读恢复检查、重试、镜像根目录、块内进度、暂停／继续、双目标独立回读及复校验均通过；原生 Intel 证据来自上述CI，Rosetta结果不冒充Intel实机。
- 获得即时确认后运行上述最终下载的arm64应用，数据目录为合成库副本，未安装或操作系统中的生产版本。实际查看0.1.22工作台、新建备份三步、项目卡片、传输源／目的地路径和内置说明：普通备份默认、301文件／1 GiB扫描、按次保存默认、镜像说明、最终路径预览、安全预检及折叠高级项一致；帮助仅在点击后打开且目录默认折叠。没有点击开始，隔离目的地保持空目录，验收前后的任务／项目记录摘要相同。
- `v0.1.22` 于 `2026-09-01T01:48:29Z` 公开并成为latest；公开Release仍指向已验收标签，四项附件名称、大小、GitHub摘要和公开下载地址与草稿阶段完全一致，四个地址均支持范围下载。没有覆盖标签、替换附件或删除旧Release。

- 最新代码078cb54的双架构预验收33431562824成功：原生arm64与原生Intel各204通过／3跳过，DMG构建和打包后备份／恢复／媒体专项通过。两份预验收DMG已完整回下载、通过磁盘镜像校验并只读挂载；版本／架构正确，包内主进程、preload、HTML、JS、CSS及图片与该提交的本地构建逐字节相同。包内媒体二进制／对应源码／许可门禁通过；本机arm64原生与x64 Rosetta再次通过三哈希、恢复、暂停继续、回读及媒体专项。它们不是最终Release附件，最终发布后还须重新验收标签流水线的准确附件。
- 用户在隔离开发窗口实际从Finder拖入一个素材源和一个目的地：界面由第1步的0/0更新为第2步的1个源／1个目标，完成100文件扫描并显示最终保存路径。没有启动传输或写入。此结果关闭先前桌面控制工具无法代替的人手拖拽门禁；下面的noWindowsAvailable记录保留为验收过程，而非当前未完成项。
- 暂停残留预计时间、恢复速度窗口和99.x%显示先建立失败回归，再修复。全量 203 项通过／3 项可选环境或性能测试跳过，类型检查、生产构建、补丁检查及补充初始界面结构回归通过；npm audit 为 0 个已知漏洞。
- 已复审普通备份／项目模式、折叠高级项、容量未知和物理关系待核对语义。静态结构测试不代替实际点击、Finder 拖拽和窗口验收；后续已在隔离开发窗口完成相关实际操作，最终发行包仍按上方门禁单独复核。
- 单独运行隔离性能样本：4 文件／256 MiB、各3次交替；预哈希复制阶段中位数2189 ms，合并读取2493 ms。后台构建及缓存会影响结果，这次没有证据宣称更快；不是 Finder 或外接物理盘基准，不据此削减校验。全部目标回读通过。
- 候选7340f2b的双架构预验收33426253412成功；随后追加“重新校验不显示旧完成总结”和单任务PDF目的地分页修订，本地全量仍为203通过／3跳过，类型检查与构建通过。该追加修订尚未完成最终桌面验收。
- 单任务PDF分页修订使用同版Electron渲染、与应用导出相同的A4/边距；1与4个合成目的地各2页，全部4页已渲染查看，标题不再孤立、容量／路径／哈希完整。该检查不代替GUI导出按钮和最终安装包操作。
- 后续解锁验收：实际普通备份301文件／1 GiB，保留源文件夹镜像302文件／9 GiB；最终路径符合预览，二者在应用回读之外再次逐文件SHA-256比对一致。镜像详情在64%暂停后速度／剩余时间清空，继续后实时到70%、99.9%及独立回读，无需重开。
- 实际GUI发现底部迷你进度仍四舍五入，新增失败回归后统一格式化；重建重启后实际项目备份中，迷你进度与详情均显示99.9%，不提前显示100%。项目301文件／1 GiB完成，项目／日期／设备／素材卷实际路径正确；旧失败卷仍在收工矩阵保留风险。
- 新建独立合成恢复库，实际执行旧UUID错误的只读检查、明确确认、重试、完整复制与独立回读；两个同盘目标均通过但只计一份副本。未确认前重试禁用，恢复不自动改写身份。
- 深色默认／1080×720与浅色默认／宽窗、长路径、折叠专业项、Tab／Shift-Tab跳过隐藏项均实际操作。d6d25c8双架构预验收33427532531成功；补充迷你进度修订后本地204项通过／3项跳过，类型和构建通过。
- Finder点击正常，但桌面控制工具发起跨应用拖拽曾报noWindowsAvailable；随后由用户在隔离开发窗口实际完成源与目的地两次拖入，界面正确更新扫描和最终路径。工具错误保留为验收过程，不再是未完成项。
- 58d9205双架构预验收33429914821成功。随后实际导出301文件长报告发现跨页卡片裁切重复表头；第一次保留卡片分片的修订在后段仍失败，未计通过。改为打印专用平面分区后，从软件按钮重新导出12页，全部逐页查看；应用之外提取PDF文本与坐标，301条路径和源哈希均恰好出现一次、各续页表头位置一致且在页边距内。类型检查、生产构建及全量204通过／3跳过，生产依赖audit为0；最终标签附件及包内字节验收已由本节上方记录补齐。

## 0.1.21 最终验收（2026-09-01）

- 双架构预验收 CI 33424350796 与最终标签 CI 33424958947 均成功，原生 arm64／原生 Intel 各 200 项通过、3 跳过；原生打包运行时通过三算法备份／恢复和媒体编解码专项。
- 最终草稿4项附件已完整回下载；两份 DMG 通过 SHA-256 与 `hdiutil verify`，只读挂载的版本／架构正确，包内主进程、preload、HTML、JS、CSS 与 v0.1.21 标签本地重建逐字节一致，双架构 app.asar 摘要相同。包内媒体来源和独立对应源码包检查通过。
- 候选实际项目 PDF 修订版3页已逐页检查，无原状态列／路径列拥挤。解锁期间已运行最终arm64 DMG：查看浅色默认／1080×720任务详情与收工矩阵；点击风险单元进入正确任务并实际完整复校验，两个同盘目标仍计一份副本；切换深色宽窗口检查代理范围工具栏。所有操作使用隔离合成库。
- 最终包实际GUI导出首次基线PDF2页并逐页查看：现存58 B、本次写入0 B、依据与哈希正确。发现目的地标题偶尔孤立在页尾的排版细节，已在0.1.22候选调整；不影响文件或校验结论。桌面再次锁屏，最终归档维护／说明入口复验未完成，Release仍保留草稿。
- 最终草稿 arm64 DMG SHA-256：`dbd0e9b2e934fa0b7d66becb2dcda73a040c9b6c69ffe35bb07430335eae5a77`。
- 最终草稿 x64 DMG SHA-256：`b06687ead9819a8920a76210b4dfd3b76842c70c8236b6f8874f9336ac716aa4`。
- 对应源码附件 SHA-256：`0d955420f950be25e2b7a4877d74b868af5dd172864e33bea4811573fbd5d885`。以上与 GitHub 资产摘要一致；仍须完成最终 GUI 后才可公开。
- 最终补验已完成：解锁后检查深色宽窗与窄窗归档维护，交接、签署、提醒、目录选择与归档操作可达，按钮不被拉高或遮挡；本页问号主动打开对应说明，侧栏进入使用说明时目录默认收起。最终arm64原生与x64 Rosetta包的三哈希备份／恢复／独立回读／重新校验和媒体专项均通过。前述锁屏阻塞解除，最终安装包达到本次公开门槛；下方历史“草稿”描述保留验收过程时间线。

- 0.1.20 未公开安装包。其双架构预验收 182 通过／3 跳过后，调用链复审发现后台覆盖统计仍按卷标识计数，取消标签构建并保留标签、不覆盖。修复后纳入 0.1.21，未将被阻断候选记作通过发布。
- 新增 5 项失败回归复现：旧卷标识误增副本、失败任务残留校验标记，以及旧额外文件确认掩盖缺失／大小／哈希差异。统一判定后通过。新增任务终态、首次基线、清单证据、分页记录兼容和报告一致性真值表。
- 实际合成项目 UI 发现紧凑日期／ISO 日期重复日单元，先复现后修复；只在读取时归一，不迁移旧任务。手动日历操作可清除旧日期别名。
- 常规回归一轮 199 项通过／3 项可选环境或性能测试跳过；随后增加“现存素材量与零新增写入”回归，最终双架构 CI 再全量复核。类型、构建、摘要／许可证门禁和差异检查通过。
- 实际桌面四类合成任务：同盘双目标、首次基线、仅结构、清单缺失；4 条记录／2 条内容验证／0 条双副本达标／4 条需处理。默认与最小窗口可查看完整解释及下一步；未操作生产素材。
- 实际从 GUI 导出项目与单任务 PDF，逐页渲染检查；修正结论／路径列宽、日期趋势和接管已保存容量。最终附件验收和发布事实将在公开前追加，当前仍为候选。

验证边界：本机原生 arm64、x64 Rosetta 与原生 Intel CI 分开记录；未完成真实 NAS、拔盘、断电、全部相机格式和新手参与者研究。没有 Developer ID 签名或公证。

## 0.1.20 候选验收（2026-09-01）

- 类型检查、生产构建、补丁检查通过；常规回归 181 项通过，3 项可选环境／性能测试跳过。npm audit 为 0 个已知漏洞。
- 收工误判先建立失败回归（9 项复现），修复后覆盖 expected、失败／暂停／取消／未验证、休息／未使用与已存在素材组合。拓扑测试覆盖同盘分区、APFS、跨检查批次、UUID 变化、未知阵列／网络和损坏记录。
- 媒体探测丢失元数据、预取消代理挂起均先复现再修复。新运行时来自锁定的 FFmpeg 9.0.1 / x264 源码；官方 FFmpeg 签名验证通过，完整源码／许可证／构建信息与包内二进制摘要门禁通过。
- 本地打包 Electron 44.0.0 的 arm64 原生及 x64 Rosetta 均完成三哈希、32 MiB／空文件、双目标独立回读、镜像目录、块级进度、暂停／恢复、只读恢复检查及重新校验。两者均通过 H.264 mp4/mov/mkv、ProRes mov 完整编解码、AAC、时长／帧率、缩略图、波形与预取消检查。
- 实际桌面使用独立合成项目：完成“只读检查 → 确认重试 → 复制 → 独立回读 → 完成”；任务详情实时变化，两个同盘目录显示两个校验目标、一份可计数副本。默认及 1080×720 窗口、侧栏滚动、代理范围和浅色项目卡片已查看；不借编译或静态渲染替代桌面检查。
- 以上是候选证据，最终 CI 安装包的回下载、DMG、版本、架构和实际 GUI 验收须在公开前另行记录。尚未将本地候选包作为公开附件。原生 Intel CI 与用户现场 Intel 物理介质测试分开表述。
- 未执行用户生产项目迁移、素材修改或原始 MHL 改写。未验证真实 NAS/拔盘/断电和全部相机格式；未做新手参与者研究。没有 Developer ID 签名或公证，不宣称零缺陷、完整硬件认证或 App Store 许可。

日期：2026-08-31；环境：当前 Apple Silicon Mac，Node.js 25.8.1，Electron 44.0.0。

## 0.1.19 构建结果

- TypeScript 类型检查、生产构建和 `git diff --check` 通过；19 个测试文件、148 项测试通过，3 项可选环境／性能测试跳过。`npm audit --audit-level=high`：0 个已知漏洞。
- 只读排查确认：历史失败任务的记录 UUID 与原目标盘当前 UUID 一致，旧实现直接查询任意子目录失败，随后将未知 UUID 当成变化。新实现解析真实挂载设备，根目录与子目录身份一致；没有为测试恢复或改写用户真实任务。
- 新增回归覆盖挂载点带空格、网络挂载文本、plist 字段、未知 UUID 与真实不匹配、旧设备标识兼容、子目录／符号链接身份、存在的最终目录身份、只读检查不改任务或文件、真实恢复复制与复校验旧状态、缺失基线拒绝通过、异常原因分类。
- 双架构 DMG 均通过 `hdiutil verify`；只读挂载后版本为 0.1.19、主程序架构符合附件名称、`app.asar` 与对应打包目录一致；挂载点已卸载。
- 使用最终打包的 Electron 运行时执行更新后的 `scripts/verify-transfer-runtime.ts`：arm64 原生和 x64 Rosetta 均通过预存 UUID 的失败任务只读检查／恢复、32 MiB 与空文件、三种哈希、双目标独立回读、镜像落点、块内进度、暂停／继续以及复校验旧错误／离线状态清理。x64 Rosetta 不等于真实 Intel 硬件测试。
- 桌面使用独立临时数据目录和生成文件。已实际查看顶栏问号、项目卡片两列按钮、长项目名和项目交接入口；已操作恢复对话框只读检查。发现并修正恢复弹窗底部布局后，Mac 锁屏阻止继续验收：最终弹窗滚动、完整 GUI 重试链、最小窗口、深浅主题仍需人工复核。自动测试不能替代这些桌面检查。
- 没有对用户素材盘执行复制、删除、修复、MHL 改写或实际任务重启；未替换系统中已安装的 Kocpy。
- 本地 arm64 DMG SHA-256：`adc4683a34c9aa75f72e064bae633e2a8fd24d13e1c4938a64d5da8f340429b5`。
- 本地 x64 DMG SHA-256：`c5ae5f333f4e298b11377c66011cd42e1c38bacbf72e888e95ea6007e1332e0d`。
- GitHub 双架构构建均通过类型检查、测试与打包。最终发布采用 CI 产物，完整回下载后两份 DMG 均通过 `hdiutil verify`、版本／架构和包内代码比较；主进程、preload 和渲染资源与本地已验证构建一致。最终包的 arm64 原生和 x64 Rosetta 运行时均再次通过上述三算法、双目标、恢复、暂停／继续及复校验专项检查，挂载点已卸载。
- 最终发布 arm64 DMG SHA-256：`ce251f543f24ed1e55012d7b8628be0fafa687fd9e30786cac5a464b60c66e97`。
- 最终发布 x64 DMG SHA-256：`7102607cb1e0d4796fd1f6f7f5103accf8cdac4af9d90eb0638285a0fe66ee63`。
- 最终摘要与 GitHub 资产摘要一致，Release 附 `Kocpy-0.1.19-SHA256SUMS.txt`。Downloads 中同版本的两份包同步为最终发布产物；初始本地构建仍保留于构建目录，不作为最终下载包。
- 无 Developer ID 签名、公证；没有执行本轮真实拔盘、睡眠、断电、SMB/NAS 或全盘长期压力测试，不宣称所有现场风险已排除。

## 0.1.18 历史结果

- TypeScript 类型检查、生产构建和 `git diff --check` 通过；18 个测试文件、133 项测试通过，3 项可选环境／性能测试跳过。
- 新增回归覆盖明确复校验范围、切换范围清除旧选择、实际签署人与必填检查项、取消结果、批量部分失败重试、自定义机位及路径规则、维护互斥／进度／结果记录、局域网索引的真实 HTTP 读取和隐私字段限制；另有初始界面服务端渲染冒烟测试。服务端渲染不等于真实桌面交互验收。
- 复校验结束与首次传输完成分别处理，复校验不会重复触发完成后代理或推出动作；素材目录写入成功后再通知界面刷新。
- `npm audit --audit-level=high`：0 个已知漏洞。
- `npm run dist` 成功生成 arm64 与 x64 两份 0.1.18 DMG。两份均通过 `hdiutil verify`；只读挂载后检查版本、主程序架构以及 `app.asar` 与对应打包目录逐字节一致，挂载点已卸载。
- 在最终打包应用的 Electron Node 运行时分别执行 `scripts/verify-transfer-runtime.ts`：arm64 原生与 x64 Rosetta 均通过 32 MiB 文件与空文件、SHA-256／SHA-1／MD5、双目标独立回读、镜像落点、块内进度、暂停／继续及源哈希／ASC MHL 验证。x64 Rosetta 不代表真实 Intel Mac 验收。
- 初始本地 arm64 DMG SHA-256：`9c5795bfa9d727fe0dd21151bcb156ab9358b62cc11add3eb2ce4dde2ea66a81`；初始本地 x64：`3a8901e54e44f438fee3e6e3867f2e06b194a917f5ebcbaca5318749e3f7609a`。它们保留在本地构建目录，不是最终公开下载包。
- GitHub 两路 CI 均通过类型检查、测试和构建。最终发布采用 CI 构建产物，已完整回下载：两份 DMG 均再次通过 `hdiutil verify`、只读挂载版本／架构检查，以及对应打包 Electron 运行时的三算法、双目标、镜像、进度和暂停／继续专项验证。
- 最终发布 arm64 DMG SHA-256：`09e91c05835c1511034fe7bafc8f75f94fad904684143534a3c4a698406fba56`。
- 最终发布 x64 DMG SHA-256：`a4555d90de42408209503c9ce6f06bad7c94b67521543f2107e6c81cdb471e2d`。
- 以上摘要与 GitHub 资产摘要一致；Downloads 中两份同版本副本已更新为最终发布产物并核验一致。Release 附 `SHA256SUMS.txt`，Downloads 附 `Kocpy-0.1.18-SHA256SUMS.txt`。
- 本轮桌面处于锁定状态，真实 Finder 拖拽、键盘焦点、深浅主题、最小窗口和用户现场介质仍需人工验收；不将源码检查和模拟渲染记作这些项目通过。
- 写入测试仅使用生成数据和独立临时目录，没有修改正式项目、素材或 MHL。当前没有有效 Developer ID Application 证书，安装包未签名、未公证。

## 0.1.17 本地历史结果（改进已纳入 0.1.18）

- TypeScript 类型检查、生产构建与 `git diff --check` 通过；117 项常规测试通过，3 项可选环境／性能测试默认跳过。
- 新增回归覆盖：新镜像保留源文件夹、旧镜像恢复沿用原落点、镜像根目录符号链接越界拒绝、三种源哈希与 ASC MHL MD5 一致、单个大文件内部字节进度、暂停／继续状态不被节流丢弃、校验 0% 暂停后正确恢复、部分写入补齐、实时详情与旧快照合并、拖拽使用 `webUtils`。
- 另行执行隔离性能对照：`KOCPY_TRANSFER_BENCH=1 npx vitest run tests/transfer-performance.integration.test.ts --reporter=verbose --disableConsoleIntercept`。本机 4 文件、256 MiB、单目标，交替进行预哈希再复制与合并读取，各 3 次；复制阶段中位数分别为 914 ms 与 853 ms，所有目标独立回读通过。该数据受本地缓存影响，不是 Finder 对照，不代表实体外接盘或 Intel 性能。
- 将 `scripts/verify-transfer-runtime.ts` 打包后，分别在两份打包应用的 Electron Node 运行时执行。arm64 原生与 x64 Rosetta 均通过 SHA-256／SHA-1／MD5、32 MiB 文件、双目标回读、镜像落点、块内进度及暂停／继续验证。x64 Rosetta 不等于 Intel 实机验收。
- `npm audit --audit-level=high`：0 个已知漏洞。
- 两份 DMG 均通过 `hdiutil verify`，打包应用版本均为 0.1.17，可执行文件分别为 arm64 与 x86_64。
- 两份 DMG 均以只读方式挂载检查：包内版本与架构正确，`app.asar` 与对应打包目录逐字节一致；已卸载本轮挂载点。Downloads 中的两份副本摘要与构建产物一致。
- 本地 x64 DMG SHA-256：`3c87598b372b26b34f59c46fdda072ed7ebd19069ed4d3748494490d76fe4f6d`。
- 本地 arm64 DMG SHA-256：`6d9ce6ee6019340c709d6bc31ea0b26eb47eae0a9bf40f74132438c4d18a4bcd`。
- 桌面验收限制：已启动独立数据目录的 x64 验收应用，但 Mac 随后锁屏，未能完成真实 Finder 拖拽与窗口交互检查。此项不记为通过，需要解锁后补验；不使用模拟结果替代实际桌面验证。
- 写入测试全部使用生成数据和独立临时目录，没有对正式素材、项目或 MHL 做修改。未覆盖本轮用户的真实外接盘速度、断连与 Intel 实机表现。
- 当前仍无有效 Developer ID Application 证书，安装包未签名、未公证。本轮生成本地验收包，没有提交或推送 GitHub。

## 0.1.16 当前结果

- TypeScript 类型检查、生产构建与补丁格式检查通过；常规自动化测试 104 项通过，2 项可选环境测试跳过。
- 活动项目与已归档项目共用受保护的内部记录删除流程；专项测试确认终态任务可清理，活动备份或代理任务会阻止删除，其他项目记录不受影响。
- 五个系统模板均提供说明、设备/前缀、副本标准、检查表和不同完成动作；旧模板可迁移，自定义模板的保存、导入和选择性应用均由主进程复核。
- 侧栏导航独立滚动，文字、图标、品牌和按钮不再随窗口高度缩小；样式回归测试禁止重新加入高度压缩断点。
- `npm audit --audit-level=high` 为 0 个已知漏洞。
- 双架构 DMG 均通过 `hdiutil verify`；挂载检查版本为 0.1.16，主程序分别为 arm64 与 x86_64。
- 本地 `Kocpy-0.1.16-arm64.dmg`：SHA-256 `897b777505255db9208be3866a7947dadb752651d593dc0192caa7e3c2519c77`。
- 本地 `Kocpy-0.1.16-x64.dmg`：SHA-256 `cffeb72d9b9368c59a7ba51aa547a08490b2120e06e469f7908915645112b3dd`。
- GitHub Release 回下载的 `Kocpy-0.1.16-arm64.dmg`：SHA-256 `686addf0cde730ff7fb956009580f5f2aeed035b72ae67d419fab47b9764b75b`。
- GitHub Release 回下载的 `Kocpy-0.1.16-x64.dmg`：SHA-256 `2dc9b65980fafa84ba7bfdc6c6ae3f5185da9e283d525a3243e354385fb4603b`。
- 两套公开 DMG 回下载后均再次通过 `hdiutil verify`；挂载检查版本为 0.1.16，主程序架构正确。
- 当前机器没有有效 Developer ID Application 证书，生成的是未签名、未公证验收包。

## 0.1.15 当前结果

- TypeScript 类型检查、生产构建与补丁格式检查通过；常规自动化测试 100 项通过、2 项可选环境测试跳过。
- 新增素材类型判定覆盖视频、照片/RAW、音频、混合素材与录音设备兜底；既有备份接管会直接保存媒体分类，旧任务从现有文件记录补全，不重新读取素材。
- SQLite 专项验证已归档项目删除会同时移除该项目的任务与文件索引，并保留其他项目；删除后的数据库持久化失败时恢复删除前内存快照。
- 主进程仅允许删除已归档且没有活动备份/代理任务的项目；删除范围限定为 Kocpy 内部配置、任务、代理与归档维护记录，代码路径不包含素材、备份目录、报告或 MHL 文件删除。
- 传输列表完整显示源路径与每个目的地的实际最终路径，路径可在 Finder 中定位，搜索同时覆盖卷名、源和目的地。
- `npm audit --audit-level=high` 为 0 个已知漏洞。
- 本地生成的双架构 DMG 均通过 `hdiutil verify`；包内版本均为 0.1.15，主程序分别为 arm64 与 x86_64。
- 本地 `Kocpy-0.1.15-arm64.dmg`：SHA-256 `0bbd45037568e7a9ff7a78d80d56faa4108f65dcdcfcb54e07c0d3a4ca742ea2`。
- 本地 `Kocpy-0.1.15-x64.dmg`：SHA-256 `e2dcd2526a9ff96affd3be022bf9e3698369db2888ca0fffa640a70d329e713e`。
- GitHub Release 回下载的 `Kocpy-0.1.15-arm64.dmg`：SHA-256 `2118e8c053189e4095e8f4ed40cd84e1d03320de16e7018bdffa52bbc91ee291`。
- GitHub Release 回下载的 `Kocpy-0.1.15-x64.dmg`：SHA-256 `5b64ea5b88e44202b217190d67aaee137a0bdabca121edd02c35317bf8025dcd`。
- 两套公开 DMG 回下载后均再次通过 `hdiutil verify`；挂载检查版本为 0.1.15，主程序架构正确。验收下载已移入废纸篓，可恢复。
- 当前机器没有有效 Developer ID Application 证书，生成的是未签名、未公证验收包。

## 0.1.14 当前结果

- TypeScript 类型检查通过；常规自动化测试 95 项通过、2 项可选环境测试跳过；`git diff --check` 通过。
- SQLite 10 万与 100 万文件压力测试均通过；百万文件测试耗时约 18.5 秒。
- `npm audit --audit-level=high` 为 0 个已知漏洞；Electron 生产构建通过。
- 新增专项覆盖：复制完成前素材源变化拒绝、文件与空目录元数据保留、损坏 JSON 不覆盖健康备份、SQLite 完整性恢复与文件记录重建、工作站路径越界拒绝、外部清单重复/越界拒绝、重名目的地 MHL 真实路径、安全推出子目录解析、诊断字段进一步脱敏。
- 双架构 DMG 均通过 `hdiutil verify`；包内版本均为 0.1.14，主程序分别为 arm64 与 x86_64。
- 从 GitHub Release 回下载的 `Kocpy-0.1.14-arm64.dmg`：SHA-256 `befb558a445f82a82a0cf85738206e5f2df1676e6d7d283fab540e5262a7ff52`。
- 从 GitHub Release 回下载的 `Kocpy-0.1.14-x64.dmg`：SHA-256 `a3a915f61391219ab79424d30588c79ad5db51ff6ce5e9a8ea912e0bdedb656c`。
- 两套公开 DMG 回下载后均再次通过 `hdiutil verify`；挂载检查版本为 0.1.14，主程序架构正确。临时下载目录验证后已移入废纸篓，可恢复。
- 当前机器没有有效 Developer ID Application 证书，生成的是 ad-hoc/未签名、未公证验收包。GitHub Workflow 已区分无证书构建与配置凭据后的签名、公证构建。
- 详细修复范围见 [0.1.14 安全与可靠性审计](SAFETY_AUDIT_0.1.14.md)。

## 0.1.13 当前结果

- 混合清单差异会明确按“补回缺失 → 完整核对 → 处理额外文件”分步显示；部分修复成功不再通过 IPC 异常呈现。
- 完整核对仍有差异时会返回并持久化最新素材卷记录、文件集合与差异状态，不会让后续基线继续读取修复前的旧记录。
- 新增带 `(1)` 重名后缀的疑似同名冲突提示，分别显示清单期望大小与额外文件实际大小；0 字节额外文件单独警告。
- 纯额外差异可在同一弹窗建立当前完整哈希基线，再经用户确认采用；Kocpy 不会自动删除或移动误放文件。
- 对真实 `20260828 / POCKET / Pocket4` 只读核对：MHL 所需的 `D(1).LRF` 为 83,918,050 字节，当前 DiskRAID 同名基础文件为 0 字节；健康副本中的有效文件与前一拍摄日副本 SHA-256 一致，确认属于真实混合差异。
- 自动化测试 89 项通过、2 项可选压力测试跳过；TypeScript 类型检查、生产构建、补丁格式检查通过；`npm audit --audit-level=high` 为 0 个已知漏洞。
- 从 GitHub Release 回下载 arm64 与 x86_64 DMG 后均通过 `hdiutil verify`；挂载后版本均为 0.1.13，主程序架构正确。远端 SHA-256 分别为 arm64 `db313e07e086e99c2c8ba31f182807716ec77d19792f399037a25b1ee9b246e0`、x64 `36718f1808612be0b6e4705bc8f7b486ddf00c8c80fe9563dd128a0dfc8129e3`。
- 此版本无 Developer ID 签名与 Apple 公证。

## 0.1.12 当前结果

- 只有纯缺失 MHL 差异允许经审计修订；额外文件、大小不同或哈希不同均会拒绝。
- 修订需要剔除原因、风险复选确认和精确文字 `修改 MHL`，界面与主进程分别校验。
- 原始 MHL 在任何修改前按 SHA-256 复制到素材卷内隐藏的 `.kocpy-manifest-history`，审计副本回读一致后才继续；隐藏审计目录不参与素材扫描。
- 修订文件必须保持原算法、准确减少确认条目并重新解析通过，随后原子替换；素材文件不会被删除。
- 修订完成后仍执行整卷重校验，可信状态明确显示排除数量，并可定位原始 MHL 审计副本。
- 专项测试 18 项通过，包含审计修订成功与非法排除拒绝；完整结果为 88 项自动化测试通过、2 项可选压力测试跳过，TypeScript 类型检查、生产构建与补丁格式检查通过。
- `npm audit --audit-level=high` 为 0 个已知漏洞。
- arm64 与 x86_64 DMG 均通过 `hdiutil verify`；挂载后版本均为 0.1.12，主程序架构正确。SHA-256 分别为 arm64 `a9decb49a9d9123c6ea1cce5b985d4031c952d3ae770f4d7550e757399a9a874`、x64 `4a39b08072e3184bc7ae6d3ea88c12bbcbee8026191362ea8adb4946790725d7`。
- 此版本无 Developer ID 签名与 Apple 公证。

## 0.1.11 当前结果

- 对“纸扎人阿岳”真实问题执行只读核对：目标素材卷的 MHL 共 376 项，当前目录有 262 项；健康来源中另外 114 个文件的名称、大小与 xxHash32 均与 MHL 完全一致。
- 确认旧版错误源于严格根目录拼接：清单路径为 `DCIM/100MSDCF`，健康副本实际重组为 `111/100MSDCF`；手动创建项目不是成因。
- 修复逻辑支持从素材卡根目录、匹配子目录或上级目录建立唯一完整映射；找不到或存在多个不同的完整映射时拒绝写入。
- 写入前完成路径、大小、哈希和空间预检；全量文件在目标卷暂存并回读校验后才提交，失败时回滚本次新增文件。
- 专项测试 16 项通过，包含重组目录成功映射和多候选歧义拒绝；完整结果为 86 项自动化测试通过、2 项可选压力测试跳过，TypeScript 类型检查、生产构建与补丁格式检查通过。
- arm64 与 x86_64 DMG 均通过 `hdiutil verify`；挂载后版本均为 0.1.11，主程序架构正确。SHA-256 分别为 arm64 `f1f40de41890ea9bca29f8b661d37ca43fb2a1531e47b93141a842f3c85edbc1`、x64 `76475594aae9c4b5c79b709696fadb02cce088a270fa69f64e4dd3ec849d54f2`。
- 软件内使用说明的模块默认全部折叠，只有用户点击标题后才展开。
- 此版本无 Developer ID 签名与 Apple 公证。

## 0.1.9 当前结果

- 窗口布局单元测试覆盖大屏、普通小屏与低于标准最小尺寸的工作区域，默认尺寸和最小尺寸均不会超过可用屏幕。
- 通过 Computer Use 使用隔离数据启动开发构建，默认 1480 × 960 窗口中全部侧栏入口与页面板块可见。
- 将隔离验收构建固定为正式最小尺寸 1080 × 720 后复查：11 个模块、偏好设置、检查更新和作者入口全部可见，主内容区保持独立滚动。
- 实测后将紧凑高度断点从 920 提高到 1000 像素，确保默认 960 像素高窗口不会裁掉底部入口。
- TypeScript、81 项自动化测试与生产构建通过，2 项可选环境压力测试跳过；`npm audit --audit-level=high` 为 0 个漏洞。
- GitHub Release 两套 DMG 回下载后均通过 `hdiutil verify`；远端 SHA-256 为 arm64 `15ec0cc6af2e2afec8230c952290aa8660a04a2a771ad0af110ecb74236dc56a`、x64 `c6a345196ce7fa105480595dee7016b301dcc37ec2f137b2f017392bdccf6c52`，挂载检查版本与架构均正确。

## 0.1.8 当前结果

- 新增父级汇总专项测试：仅当父目录的非清单文件集合与所有下层卡卷并集完全一致时才移除；没有下层卡卷记录的平面目录保持不变。
- 新增 Kocard MHL 测试：十进制 xxHash32、以 `/` 开头的路径、缺少文件与额外文件均可正确解析。
- 对当前“纸扎人阿岳”项目执行只读验证：29 条接管记录中识别 7 条父级汇总，刷新后为 22 个真实素材卷、2311 个文件、880,672,127,895 字节。
- 项目中的 17 份 MHL 有 15 份路径与大小完全一致；`20260826/A7CR/Untitled_202608262327` 缺少 114 个文件，`20260828/POCKET/Pocket4` 额外 1 个文件。
- 有真实外部清单差异的基线记录不再计入项目收工达标。
- 真实项目验证只读取素材与当前数据库，未写入或修改任何用户数据。
- TypeScript、78 项自动化测试与生产构建通过，2 项可选环境压力测试跳过；`npm audit --audit-level=high` 为 0 个漏洞。
- arm64 与 x86_64 DMG 均通过 `hdiutil verify`，包内版本为 0.1.8，主程序架构正确。
- 从 GitHub Release 回下载两套公开 DMG 后再次通过 `hdiutil verify`；远端 SHA-256 为 arm64 `6abdc1fa2ed30adfd539eea352ba68c9565a0b438a5b2d2f4d0efc17d9dabe07`、x64 `6e8c333f4aae7aeec735d49c11bbe282a602b40ede10c6b415bb16f79c8f5e08`，挂载检查版本与架构均正确。

## 0.1.7 当前结果

- 同路径重复接管专项测试覆盖无哈希导入、清单失败和多次首次基线混合记录，5 条历史记录可归并为 1 条可信素材卷。
- 不同路径只有全部文件哈希一致时才合并；无哈希的不同路径记录保持独立。
- 使用当前“纸扎人阿岳”项目数据进行只读内存模拟：108 条接管记录对应 29 个唯一素材卷路径，可清理 79 条重复记录。
- `20260825 / FX3 / Untitled_202608260150` 模拟刷新结果由 5 个素材卷、380 个文件、94,448,006,370 字节恢复为 1 个素材卷、76 个文件、18,889,601,274 字节，收工结果由 2/5 恢复为 1/1。
- 模拟过程没有写入用户项目数据库或素材文件。
- 完整类型检查通过；自动化测试 69 项通过、2 项按环境跳过；`npm audit --audit-level=high` 报告 0 个漏洞。
- arm64 与 x86_64 应用均显示 `0.1.7`，可执行文件架构正确，两个 DMG 均通过 `hdiutil verify`。
- 在隔离的数据副本与未打包的 arm64 应用中确认 `0.1.7` 界面；素材卷明细四列表头和各行内容保持纵向对齐。系统随后锁屏，刷新写入流程由只读真实数据模拟与自动化测试覆盖。
- 软件内使用说明新增 0.1.7 更新提示与“刷新既有项目”专章；GitHub 新增完整用户手册，覆盖接管层级、可信度、刷新边界、项目收工状态和故障排查。
- 说明修订后再次完成类型检查、生产构建与自动化测试，69 项通过、2 项按环境跳过。
- 从 GitHub Release 重新下载包含新版说明的两套公开 DMG 后再次通过 `hdiutil verify`；远端 SHA-256 为 arm64 `cf8a704d4c96a3b9c3d9e0807814f2ef15de4d0fed068378ebfaba93c9b9e076`、x64 `3ab6686f749dc5a8a387af3f2195031692498fecdb7864d101a8ffcbc02e0c72`。

## 0.1.6 当前结果

- TypeScript、66 项常规自动测试、生产构建全部通过；2 项可选压力测试默认跳过。
- 既有备份测试覆盖逐文件接管进度回调、目录结构导入的独立未验证状态，以及项目配置外设备名称保留。
- 项目收工测试覆盖“当天未发现素材，待确认”和用户明确标记“应该有素材，缺少备份”两种互不混淆的状态。
- 项目外识别设备只加入实际发现它的拍摄日，不再扩展为整个拍摄周期的空白单元。
- `npm audit --audit-level=high` 为 0 个已知漏洞。
- `npm run dist` 生成 arm64 与 x64 `.app` 和 `.dmg`；两份 DMG 均通过 `hdiutil verify`，包内版本均为 0.1.6，主程序架构分别为 arm64 与 x86_64。
- GitHub Release 的两份资产已完整回下载；下载文件均再次通过 `hdiutil verify`，SHA-256 与 GitHub 资产摘要一致。
- 已安装的 0.1.5 与验收构建使用相同应用标识，macOS 会将第二次启动重定向至已安装应用；为避免覆盖用户应用与项目数据，本轮没有进行写入式桌面操作，界面状态由类型检查、构建和组件逻辑检查覆盖。
- 此版本无 Developer ID 签名与 Apple 公证。

## 0.1.5 当前结果

- TypeScript、63 项常规自动测试、生产构建全部通过；2 项可选压力测试默认跳过。
- 新增项目、单日和单卡接管结构测试，覆盖任意素材卷名称、A–E 机位、自定义目录规则与无效日历日期。
- 项目收工矩阵测试覆盖同设备多机位独立统计与物理副本去重。
- `npm run dist` 生成 arm64 与 x64 `.app` 和 `.dmg`；两份 DMG 均通过 `hdiutil verify`，包内版本均为 0.1.5，主程序架构分别为 arm64 与 x86_64。
- 此版本无 Developer ID 签名与 Apple 公证。

## 0.1.4 当前结果

- TypeScript、59 项常规自动测试、生产构建全部通过；2 项可选压力测试默认跳过。
- 真实外置 APFS 双盘压力通过：每盘 256 MiB 大文件 + 1,500 个小文件，逐目标 SHA-256 回读校验。
- 隔离可写磁盘镜像 APFS、exFAT、HFS+ 压力测试全部通过，测试目录与镜像均已清理。
- SQLite 索引 10 万与 100 万文件压力测试通过。
- `npm audit`（含开发依赖）为 0 个已知漏洞。
- Electron 44、electron-vite 5、Vite 7 工具链构建通过。

以下 0.1.0 记录保留作为历史基线；本文件末尾仍列出的 Developer ID 与无可用 SMB 测试共享项不属于代码实现缺失。

## 自动检查

- `npm run typecheck`：通过。
- `npm test`：9 个测试文件、59 个测试全部通过，2 个可选压力测试默认跳过。
- 临时挂载卷压力测试：APFS 与 exFAT 双目的地通过；256 MiB 大文件、1,500 个小文件、逐目标 SHA-256 回读校验，耗时 91.26 秒。
- `npm run dist`：通过；生成 arm64 与 x64 `.app` 和 `.dmg`，两份 DMG 均通过 `hdiutil verify`，包内版本均为 0.1.4，主程序架构分别为 arm64 与 x86_64。
- 原 `.icns` 图标与 Kocpy 图标 SHA-256 完全一致。

## 自动测试覆盖

1. 两目标真实拷贝、空文件、空目录、源文件修改时间不变。
2. 第一目标已存在同内容文件，仍能完成第二目标，且完整校验。
3. 同名不同内容不覆盖；其他目标可成功，任务整体明确失败。
4. 同名冲突创建带后缀的新副本。
5. 拒绝源/目标相同、嵌套和符号链接别名。
6. 拒绝通过目标内部符号链接写回源。
7. 源含符号链接明确报错，不静默跳过。
8. 取消不显示成功；重试重新校验且计数正确。
9. 拒绝重复启动与删除进行中任务。
10. 失败任务不阻塞队列后续任务。
11. 目录备份保留目的地额外文件。
12. 项目/日期/机位目录与唯一备份目录。
13. 空源不能显示为已校验成功。
14. 拒绝不支持的哈希算法和零目的地。
15. 不支持硬链接时，模拟 ENOTSUP，走真实独占复制回退路径。
16. 清理并恢复本任务上次中断留下的临时文件。
17. 并发持久化串行执行，损坏主文件能从上一份 .bak 恢复。
18. HTML 报告正确转义不可信名称，失败状态不伪装成功。
19. 内置 FFmpeg 实际生成 H.264 与 ProRes Proxy，再完整解码验证。
20. 旧 JSON 任务与项目可重建 SQLite 索引，分页检索和损坏索引备份恢复通过。
21. 既有备份目录扫描、外部基线接管、可信度标记与项目覆盖统计通过。

## 桌面应用实际操作

- 使用打包后的 `Kocpy.app`（非浏览器假接口）打开全部七个页面。
- 用生成的 640×360 测试视频与文本文件，从三步向导发起一次双目标备份。两个目标均 SHA-256 一致。
- 检查任务详情、目的地路径、校验日志与素材库清单。
- 从素材库使用打包内置 FFmpeg 生成 H.264 代理，返回实际文件路径。
- 创建项目、归档项目，重启后记录保留。
- 深色/浅色主题保存、切换，最后恢复深色。
- 原生保存对话框导出 PDF；检查中文和长哈希/路径表格排版。
- 最终构建重新启动，任务历史恢复正常；二次启动进程立即退出，仅保留一个主实例。
- 仅使用自建测试文件，未向用户正式素材或原两个项目写入。

本机界面中保留了一条明确命名为「功能自检 · 双目标备份」的真实测试记录和一个已归档测试项目，方便查看完成态。安装包本身不包含这些用户数据。

## 仍需特定外部条件的验收

- 实际外置 APFS 双盘已经通过；实体 exFAT 介质、SMB/NFS 共享、不同读卡器、安全推出、物理拔盘与断电仍需对应的可牺牲硬件或测试服务器。
- 百万文件数据库压力已经通过；数 TB 素材的长时间实体传输、网络重连及真实磁盘故障仍需专用测试窗口。
- Intel Mac、所有相机专有 RAW 视频编解码、剪辑软件时间码往返。
- Apple Developer ID 签名与 Apple 公证需等待有效证书；构建、双架构打包、DMG 校验和依赖合规检查已经完成。

无硬链接文件系统的回退和挂载 exFAT 镜像均已测试，但仍不等于实体 exFAT 介质验证。建议正式使用前先以有其他副本的素材进行小批量测试。
